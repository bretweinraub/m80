#! /usr/bin/perl -I/usr/local/share/m80/lib/perl 
use m80::generator::webGen;
my $dbName = "mydb";
my $dbUser = "bweinraub";
my $dbPass = "bweinraub";
my $dbHost = "localhost";
my $dbPort = 5432;

webGen::genModuleListPage (dbName => $dbName, 
			   dbUser => $dbUser, 
			   dbPass => $dbPass, 
			   dbHost => $dbHost,
			   dbPort => $dbPort);

