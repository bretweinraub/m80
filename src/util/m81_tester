#!/usr/bin/perl

use Data::Dumper;
use m80::base;

#print Dumper(\%{*{Getopt::Long::}});
opt(%options);
debugprint "TESTING!\n";

use m80::debugger;
use m80::repositoryLoader;

#&dump_functions();

my $d = new m80::repositoryLoader;
print Dumper($d);
exit 0;